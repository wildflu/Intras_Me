

import 'package:flutter/material.dart';

class HomeINdex extends StatelessWidget {
  const HomeINdex({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Index home"),
      ),
    );
  }
}