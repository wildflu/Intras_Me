



import 'package:flutter/material.dart';

class ShareIndex extends StatelessWidget {
  const ShareIndex({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("share home"),
      ),
    );
  }
}